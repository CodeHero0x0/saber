# Plan — SABER-20260723-003

## Saber 本仓

1. 更新 `AGENTS.md`、`CLAUDE.md`、`workflows/requirements/SKILL.md` 关闭推送约定。
2. `git add`：`workitems/SABER-20260723-001`、`002`、`003` 与文档变更。
3. Commit 后 `git push origin main`。
4. 用 `gh`/`git ls-tree` 只读确认远程存在目标路径。

## Verification

- `git status` 干净（相关路径）
- 远程 path 存在 `workitems/SABER-20260723-001` 与 `002`
- 文档含约定原文可检索
