# BA 需求澄清产出 - DEMO-101

BA 对照 `intake.md` 澄清后确认：订单备注最多接受 200 个 Unicode 字符。前端展示剩余字符数并阻止超长提交；后端对超长输入返回 `ORDER_NOTE_TOO_LONG`。空备注或省略备注继续有效，首尾空白不自动删除。

## 已确认范围

- 0、1、199、200 个字符的备注能够成功提交。
- 201 个字符的备注在前端被阻止，绕过前端时由后端一致拒绝。
- 未填写备注的存量订单和调用方保持兼容。

## BA 阶段结论

范围、非范围和三条可观测验收标准已确认，可以进入 Dev 构建阶段。
