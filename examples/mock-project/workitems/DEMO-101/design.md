# Dev 技术设计产出 - DEMO-101

后端是 200 字符规则和稳定错误码的权威实现。前端镜像该上限以提供即时反馈，同时保留展示后端校验错误的能力。现有字段容量满足要求，无需数据库迁移。

## 接口约定

- 后端按 Unicode 字符数判断，超过上限返回 `ORDER_NOTE_TOO_LONG`。
- 前端计数规则与后端一致，剩余字符不得小于 0。
- 请求字段保持可选，不改变既有客户端契约。

## 风险与回滚

主要风险是前后端计数口径不一致。发布按后端、前端顺序进行；出现问题时分别回滚 `repositories.yaml` 中记录的虚构提交。
