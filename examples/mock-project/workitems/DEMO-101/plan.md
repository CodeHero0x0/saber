# Dev 实施与修复产出 - DEMO-101

## 首轮实施

1. 增加后端边界校验和契约测试。
2. 增加前端计数器和客户端边界测试。
3. 分别运行仓库测试，再联合验证 200/201 字符场景。
4. 将分支、提交、MR 和 CI 引用写入 `repositories.yaml`。

## QA 失败后的修复

QA 发现后端使用了 `>= 200`，导致恰好 200 字符被拒绝。Dev 将条件修正为 `> 200`，补充 199/200/201 三组回归用例，并重新交给 QA 验证。
