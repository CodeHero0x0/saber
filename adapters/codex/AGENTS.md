# Codex adapter

Codex discovers repository skills from `.agents/skills`. Saber materializes only the selected
role's complete team, workflow, and external skill packages there. The generated `saber--context`
skill identifies the active role, project, capabilities, and recovery rules.
